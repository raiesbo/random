describe('Test Get /launches', () => {
    it('should respond with 200 success status', () => {
        const response = 200;
        expect(response).toEqual(200);
    })
})

describe('Test POST /launches', () => {
    it('should respond with 200 success status', () => {
        const response = 200;
        expect(response).toEqual(200);
    })

    it('should catch missing required properties', () => {

    })

    it('should catch invalid dates', () => {
        
    })
})